package com.pixelpunch.passflow.service.PasswordService;

import com.pixelpunch.passflow.model.Password.Password;
import org.springframework.stereotype.Service;
import com.pixelpunch.passflow.service.interfaces.PasswordInterfaces.HistoryPasswords;

@Service
public class HistoryPasswordsServiceImpl implements HistoryPasswords {
//    public void addPasswordHistory(Password generatedPassword) {й
//        HistoryPasswords passwordHistory = new PasswordHistory();
//        passwordHistory.setPassword(generatedPassword.getPassword());
//        passwordHistory.setCreationDate(LocalDateTime.now());
//
//        // Сохранение записи в базе данных
//        passwordHistoryRepository.save(passwordHistory);
    //   }

}
